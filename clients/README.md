# Clients

## Node / JS

The Node / JS client is generated from the OpenAPI specification using the `openapi-typescript` tool.

## Rust

The Rust crate is manually written to properly deal with the stream types.
